# blendit-chat

This directory is reserved for the future NestJS chat server.

Current status:
- Java/Spring Boot chat module has been removed from Gradle modules.
- No runtime server code is included yet.
- The root Gradle build and current GitHub Actions workflow only target `blendit-api`.

Next step:
- Initialize NestJS in this directory.
- Add a separate chat build/deploy flow when the chat server is ready to run.
